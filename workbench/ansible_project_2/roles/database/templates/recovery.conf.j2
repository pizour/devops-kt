# Recovery configuration for replica - Generated by Ansible
standby_mode = 'on'
primary_conninfo = 'host={{ replication_source }} port={{ database_port | default(5432) }} user=replicator'
trigger_file = '/tmp/postgresql.trigger.5432'
